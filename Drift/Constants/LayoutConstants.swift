//
//  LayoutConstants.swift
//  Drift
//
//  Shared layout constants for consistent spacing
//

import SwiftUI

enum LayoutConstants {
    /// Height of the custom tab bar including padding
    static let tabBarHeight: CGFloat = 80

    /// Bottom padding to use for content that needs to clear the tab bar
    static let tabBarBottomPadding: CGFloat = 90
}
